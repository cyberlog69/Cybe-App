name: cybe_relay
description: Standalone BitMesh LAN Relay Daemon & GitHub Updater for Windows and Linux
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.5.0
  crypto: ^3.0.3
  http: ^1.2.2
  intl: ^0.19.0
  path: ^1.9.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
